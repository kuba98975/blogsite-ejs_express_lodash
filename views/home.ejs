<%- include("partials/header"); -%>

<h1>Home</h1>

<p><%= startingContent %>!</p>

<!-- for (var i = 0; i < posts.length; i++) {
console.log(posts[i].title);
};
zamiast for loop stosuje forEach -->
<!-- Zerknij że pierwsza i ostatnia linia to scriplet kodu js a pomiędzy generuje zawartość htmla, są tam różne tagi -->

<% posts.forEach(function(post) { %>
    <h1><%= post.title %></h1>
    <p><%= post.content.substring(0,100) + "..." %><a href="/post/<%=post.title %>"> Read More</a></p>
<% }) %>

<%- include("partials/footer"); -%>
